# R11 Energy Monitor dApp

React dashboard for the `R11` energy monitor module. It follows the same wallet
and network setup used in `R9` and `R10` and polls the local bridge for the
latest reading summary.

## Quick Start

```bash
pnpm install
cp .env.example .env
pnpm dev
```
