# Package address after deploying the Move contract
VITE_PACKAGE_ID=0x...

# Queue ID after creating a queue with the client
VITE_QUEUE_ID=0x...

# WebSocket server URL (defaults to ws://localhost:8080)
# Use wss:// for production deployments over HTTPS
VITE_WS_URL=ws://localhost:8080
